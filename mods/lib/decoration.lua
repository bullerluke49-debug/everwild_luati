


function create_block(name,decoration_type,place_on,y_min,y_max, sidelen, fill_ratio, decoration, param2)
    
    core.decoration({name = name, 
    deco_type = decoration_type, 
    place_on = place_on, 
    y_min = y_min, y_max = y_max, sidelen = sidelen, 
    fill_ratio = fill_ratio, 
    decoration = decoration, 
    param2 = param2})

end
    

